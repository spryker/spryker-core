{{ 'availability_subscription.mail.notification.head'|trans({'%name%': mail.availabilitySubscriptionMailData.productAttributes.name}) }}
{{ 'availability_subscription.mail.notification.body'|trans({'%name%': mail.availabilitySubscriptionMailData.productAttributes.name}) }}

{{ mail.availabilitySubscriptionMailData.productAttributes.name }}

{% for name, value in mail.availabilitySubscriptionMailData.productConcrete.attributes if name %}
    {{ ('product.attribute.' ~ name) | trans }}: {{ value }}
{% endfor %}

{{ mail.availabilitySubscriptionMailData.productAttributes.price ?? '' }}

{{ 'availability_subscription.mail.footer'|trans({'%link%': mail.availabilitySubscriptionMailData.availabilityUnsubscriptionLink})|raw }}
