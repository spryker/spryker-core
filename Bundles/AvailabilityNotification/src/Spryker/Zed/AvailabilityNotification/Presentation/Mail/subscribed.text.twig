{{ 'availability_subscription.mail.subscribed.head'|trans|raw }}

{{ mail.productAttributes.name }}

{{ mail.productAttributes.price ?? '' }}

{% for name, value in mail.product.attributes if name %}
    {{ ('product.attribute.' ~ name) | trans }}: {{ value }}
{% endfor %}

{{ 'availability_subscription.mail.footer'|trans({'%link%': mail.availabilityUnsubscriptionLink})|raw }}
