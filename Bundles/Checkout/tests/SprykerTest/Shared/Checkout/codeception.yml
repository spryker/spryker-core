namespace: SprykerTest\Shared\Checkout

paths:
    tests: .
    data: ../../../_data
    support: _support
    output: ../../../_output

coverage:
    enabled: true
    remote: false
    whitelist: { include: ['../../../../src/*'] }

suites:
    Shared:
        path: .
        actor: CheckoutSharedTester
        modules:
            enabled:
                - Asserts
