{% extends '@Gui/Layout/layout.twig' %}

{% set widget_title = 'Update role' | trans %}

{% block action %}
    {{ backActionButton(app.request.headers.get('referer'), 'Back' | trans) }}
{% endblock %}

{% block content %}

    {% embed '@Gui/Partials/widget.twig' %}

        {% block widget_content %}

            {{ form_start(companyRoleEditForm) }}

            {{ form_row(companyRoleEditForm) }}
            <input type="submit" class="btn btn-primary safe-submit" value="{{ 'Submit' | trans }}" />

            {{ form_end(companyRoleEditForm) }}

        {% endblock %}

    {% endembed %}

{% endblock %}
