version: 1

defaults:
    destination: '{timestamp}/{data_entity}.{extension}'
    overwrite: true
    operation: export
    format:
      type: csv
    connection:
      type: local
      params:
        export_root_dir: '{application_root_dir}/data/export'
