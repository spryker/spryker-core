<div class="tab-content">
    {% for localeCode in localeCollection %}
        {% set formKey = formPrefix ~ '_' ~ localeCode %}
        {% set activeCss = '' %}

        {% if activeLocale == localeCode %}
            {% set activeCss =  'active' %}
        {% endif %}

        <div id="content_form_tab_{{ formKey }}" class="tab-pane {{ activeCss }}">
            {% block tabContentRow %}
                <div class="panel-body">
                        {% if form is defined and form[formKey] is defined %}
                            {% for input in form[formKey] %}
                                {% block formInput %}{{ form_row(input) }}{% endblock %}
                            {% endfor %}
                        {% endif %}
                </div>
            {% endblock %}
        </div>
    {% endfor %}
</div>