<div class="btn-group dropdown">
    {# @deprecated html attribute "data-toggle" is deprecated use "data-bs-toggle" instead #}
    <button data-toggle="dropdown" data-bs-toggle="dropdown" class="btn btn-xs btn-outline {{ class }} dropdown-toggle" aria-expanded="true" {{ parameters }}>{{ icon | raw}} {{ title | trans }}
        <span class="fa fa-angle-down"></span>
    </button>
    <ul class="dropdown-menu">
        {% for button in nestedButtons %}
            {% if button.needDivider %}
                <li class="divider"></li>
            {% endif %}
            <li>
                <a href="{{ button.url }}" {{ button.params }}>{{ button.title | trans }}</a>
            </li>
        {% endfor %}
    </ul>
</div>
