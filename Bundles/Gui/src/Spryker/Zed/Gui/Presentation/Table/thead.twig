{% if config.header is defined %}
<thead>
    <tr>
        {% for id, value in config.header %}
        <th id="{{ id }}" data-qa="{{ id }}">
            {{ value | trans }}
        </th>
        {% endfor %}
    </tr>
</thead>
{%  endif %}
