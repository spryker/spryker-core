{#
  Render tabs in zed UI. The `tabs()` twig function is registered globaly with `\Spryker\Zed\Gui\Communication\Plugin\Twig\TabsFunction`.

  Parameters:
    * zedTabsViewTransfer (`\Generated\Shared\Transfer\ZedTabsViewTransfer`): transfer object that contains all information of the tabs (required).
    * context (array):  Context variables that will be available in included templates (optional).
#}
<div class="tabs-container {% if zedTabsViewTransfer.isNavigable %}tabs-navigable{% endif %}">
    <ul class="nav nav-tabs">
        {% for tab in zedTabsViewTransfer.tabs %}
            <li class="{% if zedTabsViewTransfer.active == tab.name %}active{% endif %} {% if tab.hasError %}error{% endif %}">
                <a href="#tab-content-{{ tab.name }}" data-toggle="tab">{{ tab.title }}</a>
            </li>
        {% endfor %}
    </ul>

    <div class="tab-content">
        {% for tab in zedTabsViewTransfer.tabs %}
            <div id="tab-content-{{ tab.name }}" class="tab-pane {% if zedTabsViewTransfer.active == tab.name %}active{% endif %}">
                <div class="panel-body">
                    {% include tab.template %}
                </div>
            </div>
        {% endfor %}
    </div>

    {% if zedTabsViewTransfer.footerTemplate or zedTabsViewTransfer.isNavigable %}
        <div class="tabs-footer">
            {% if zedTabsViewTransfer.footerTemplate %}
                {% include zedTabsViewTransfer.footerTemplate %}
            {% endif %}

            {% if zedTabsViewTransfer.isNavigable %}
                <span class="tabs-navigation pull-right">
                    <a href="#" class="btn btn-tab-previous btn-outline btn-view"><i class="fa fa-chevron-left"></i> {{ 'Previous' | trans }}</a>
                    <a href="#" class="btn btn-tab-next btn-outline btn-view">{{ 'Next' | trans }} <i class="fa fa-chevron-right"></i></a>
                </span>
            {% endif %}
        </div>
    {% endif %}
</div>
