
{% if lastForm.vars.id == 'voucher' %}
    {% set submitBtnValue = 'Redeem' | trans %}
{% else %}
    {% set submitBtnValue = 'Submit' | trans %}
{% endif %}

{% if lastForm.vars.id == 'summary' %}
    {% set nextStepBtnValue = 'Submit Order' | trans ~ quoteType %}
{% else %}
    {% set nextStepBtnValue = 'Next Step ->' | trans %}
{% endif %}

{%
    if lastForm.vars.id != firstForm.vars.id
%}
    <input type="submit" class="btn btn-info safe-submit" value="{{ '<- Prev Strep' | trans }}" name="{{ prevStepName }}" />
{% endif %}

{%
    if lastForm.vars.id != 'products'
      and lastForm.vars.id != 'summary'
%}
    <input type="submit" class="btn btn-primary safe-submit" value="{{ submitBtnValue }}" />
{% endif %}

<input type="submit" class="btn btn-info safe-submit" value="{{ nextStepBtnValue }}" name="{{ nextStepName }}" />
