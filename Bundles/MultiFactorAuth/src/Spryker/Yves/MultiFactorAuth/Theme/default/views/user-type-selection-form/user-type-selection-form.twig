{% extends template('widget') %}

{% define data = {
    form: _view.form,
    title: 'multi_factor_auth.method.select' | trans,
} %}

{% block body %}
    {% include molecule('multi-factor-authentication-content', 'MultiFactorAuth') with {
        data: {
            form: data.form ? data.form : null,
            title: data.title,
            url: path('multiFactorAuth/getUserEnabledTypes'),
            modifiers: ['type-selection'],
        },
    } only %}
{% endblock %}
