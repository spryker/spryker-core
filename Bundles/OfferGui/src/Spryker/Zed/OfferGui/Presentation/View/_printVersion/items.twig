<div class="container">
    <h3>Offer Items</h3>
    <table class="table table-bordered table-striped table-sm">
        {% include '@OfferGui/View/_printVersion/item-table-head.twig' %}
        {% for item in quote.items %}
            <tr>
                {% embed '@OfferGui/View/_printVersion/order-item.twig' with {item: item, displayTotals: true} %}{% endembed %}
            </tr>
            {% embed '@OfferGui/View/_printVersion/order-item-option.twig' with {item: item} %} {% endembed %}
        {% endfor %}
    </table>
</div>
