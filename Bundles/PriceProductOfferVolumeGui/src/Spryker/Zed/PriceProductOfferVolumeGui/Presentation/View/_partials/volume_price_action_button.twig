{% if priceData['volume_prices'] is not null %}
    {{ viewActionButton(
        url('/price-product-offer-volume-gui/view', {'id-product-offer': idProductOffer, 'store-name': storeName, 'currency-code': currencyCode}),
        'View Offer Volume Price' | trans) }}
{% endif %}
