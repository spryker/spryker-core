{% extends '@Gui/Layout/layout.twig' %}

{% set widget_title = 'Are you sure about deleting this scheduled price?' | trans %}

{% block head_title %}
    {{ widget_title }}
{% endblock %}
{% block section_title %}
    {{ widget_title }}
{% endblock %}

{% block action %}
    {{ backActionButton(app.request.headers.get('referer')~'#tab-content-scheduled_prices', 'Back' | trans) }}
{% endblock %}

{% block content %}

    {% embed '@Gui/Partials/widget.twig' %}

        {% block widget_content %}

            <a href="{{ app.request.headers.get('referer')~'#tab-content-scheduled_prices' }}" class="btn btn-back">{{ 'No, I want to keep this price' | trans}}</a>
            <a href="{{ url('/price-product-schedule-gui/delete/confirm', {
                'id-price-product-schedule': idPriceProductSchedule,
                'redirectUrl': app.request.headers.get('referer')~'#tab-content-scheduled_prices',
            }) }}" class="btn btn-danger safe-submit">{{ 'Yes, delete this price' | trans}}</a>

        {% endblock %}

    {% endembed %}

{% endblock %}
