<span>{{ priceToPay | money(true, order.currencyIsoCode) }}</span>
{% if (subtotal - priceToPay > 0) > 0 %}
  <span {% if (subtotal - priceToPay > 0) %} class="discounted" {% endif %}>({{ subtotal | money(true, order.currencyIsoCode) }})</span>
{% endif %}
