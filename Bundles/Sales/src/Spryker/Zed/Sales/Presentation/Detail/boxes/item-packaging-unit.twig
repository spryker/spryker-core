{% if orderItem.amountSalesUnit is defined and orderItem.amountSalesUnit %}
    x {{ (orderItem.amount | trim / orderItem.quantity) | executeFilterIfExists('formatFloat', app.locale) }}
    {{ orderItem.amountSalesUnit.productMeasurementBaseUnit.productMeasurementUnit.name | trans }}
    ( =
        {{ ((orderItem.amount | trim / orderItem.quantity) / orderItem.amountSalesUnit.conversion) | executeFilterIfExists('formatFloat', app.locale) }}
        {{ orderItem.amountSalesUnit.productMeasurementUnit.name | trans }}
    )
    = {{ (orderItem.amount | trim / 1) | executeFilterIfExists('formatFloat', app.locale) }}
    {{ orderItem.amountSalesUnit.productMeasurementBaseUnit.productMeasurementUnit.name | trans }}
{% endif %}
