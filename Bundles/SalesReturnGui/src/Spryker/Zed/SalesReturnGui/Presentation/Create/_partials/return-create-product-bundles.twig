{% if form.returnBundleItems is defined %}
    {% for returnBundleItem in form.returnBundleItems %}
        {% include '@SalesReturnGui/Create/_partials/return-create-product-bundle.twig' with {
            returnBundleItem: returnBundleItem,
            bundleProduct: returnBundleItem.vars.value.bundleProduct,
            bundleItems: returnBundleItem.vars.value.bundleItems,
            order: order,
        } only %}
    {% endfor %}
{% endif %}
