{% include '@SalesReturnGui/Return/_partials/return-create-item.twig' with {
    returnItem: returnBundleItem,
    orderItem: bundleProduct,
    order: order,
    isBundleProduct: true,
} only %}

{% for orderItem in bundleItems %}
    {% include '@SalesReturnGui/Return/_partials/product-bundles.twig' with {
        orderItem: orderItem,
        order: order,
    } only %}
{% endfor %}
