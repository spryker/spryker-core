
{% extends '@Gui/Layout/layout.twig' %}

{% block section_title ' View' | trans %}

{% block action %}
    {{ backActionButton('/tax/set/list', 'Back to list' | trans) }}
    {{ removeActionButton(url('/tax/set/delete', {'id-tax-set': taxSet.idTaxSet}), 'Delete' | trans) }}
    {{ editActionButton(url('/tax/set/edit', {'id-tax-set': taxSet.idTaxSet}), 'Edit' | trans) }}
{% endblock %}

{% block content %}

    {% embed '@Gui/Partials/widget.twig' with { widget_title: 'View' | trans } %}

        {% block widget_content %}
            <table class="table">
                <tr>
                    <td>Name</td>
                    <td>{{ taxSet.name }}</td>
                </tr>
                <tr>
                    <td>Id</td>
                    <td>{{ taxSet.idTaxSet }}</td>
                </tr>
                <tr>
                    <td>Tax rates</td>
                    <td>
                        <strong>
                        {% for tax in taxSet.taxRates %}
                             {% if tax.country %}
                                 {{ tax.country.name }}
                             {% else %}
                                 {{ 'N/A' | trans }}
                             {% endif %}
                               - {{ tax.rate }} % <br />
                        {% endfor %}
                        </strong>
                    </td>
                </tr>
            </table>
        {% endblock %}

    {% endembed %}

{% endblock %}
