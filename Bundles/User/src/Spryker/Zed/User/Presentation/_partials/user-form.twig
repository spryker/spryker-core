{% for field in userForm.children %}

    {% if field.vars.name == 'is_agent' %}
        {% include '@User/_partials/user-form-is-agent-field.twig' %}
    {% else %}
        {{ form_row(field) }}
    {% endif %}

{% endfor %}
