{% for field in userForm.children %}

    {% if userForm.is_agent is defined and field == userForm.is_agent %}
        {% include '@Agent/_partials/user-form-is-agent-field.twig' %}
    {% else %}
        {{ form_row(field) }}
    {% endif %}

{% endfor %}
