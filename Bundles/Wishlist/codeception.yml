namespace: Wishlist

suite: default

paths:
    tests: tests
    log: tests/_log
    data: tests/_data
    support: tests/_support

settings:
    bootstrap: _bootstrap.php
    colors: true
    memory_limit: 1024M
    log: true

modules:
    config:
        Db:
            dsn: ''
            user: ''
            password: ''
            dump: tests/_data/dump.sql

coverage:
    enabled: true
