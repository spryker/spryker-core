{% include '@SelfServicePortal/_partials/table-file-uploader.twig' with {
    formPart: fileAttachForm.model_file_upload,
    urlDownloadExample: urlDownloadModelExample,
} %}

{{ tabs(modelAttachmentTabsViewTransfer, {
    'fileAttachForm': fileAttachForm,
    'unattachedSspModelTable': unattachedSspModelTable,
    'urlDownloadExample': urlDownloadModelExample,
    'idFile': idFile,
}) }}

{{ tabs(attachedModelsTabsViewTransfer, {
    'fileAttachForm': fileAttachForm,
    'attachedSspModelTable': attachedSspModelTable,
    'idFile': idFile,
}) }}
