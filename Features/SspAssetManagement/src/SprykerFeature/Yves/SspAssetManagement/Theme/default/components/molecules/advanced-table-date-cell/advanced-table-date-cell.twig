{% extends model('component') %}

{% define config = {
    name: 'advanced-table-date-cell',
} %}

{% define data = {
    column: '',
    config: {
        options: {
            format: '',
        },
    },
} %}

{% block body %}
    {{ data.column | date(data.config.options.format | default(null)) }}
{% endblock %}
