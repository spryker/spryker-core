{% extends template('widget') %}

{% define data = {
    isDisabled: _widget.isDisabled,
    isVisible: _widget.isVisible,
    product: _widget.product
} %}

{% block body %}
    {% if data.isVisible %}
        {% include molecule('asset-finder', 'SspAssetManagement') with {
            class: 'spacing-bottom--big',
            data: {
                formFieldName: 'assetReference',
                isDisabled: data.isDisabled,
                placeholder: 'ssp_asset_management.asset.selector.search_placeholder' | trans
            }
        } only %}
    {% endif %}
{% endblock %}
