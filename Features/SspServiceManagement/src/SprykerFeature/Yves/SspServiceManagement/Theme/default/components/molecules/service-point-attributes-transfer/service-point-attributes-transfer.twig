{% extends model('component') %}

{% define config = {
    name: 'service-point-attributes-transfer',
    tag: 'service-point-attributes-transfer',
} %}

{% define attributes = {
    'trigger-class-name': '',
    'transferable-data': '',
} %}
