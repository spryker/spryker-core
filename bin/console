#!/usr/bin/env php
<?php

use SprykerFeature\Shared\Library\Autoloader;

if (is_dir($vendor = __DIR__ . '/../../vendor')) {
    require($vendor . '/spryker/zed-package/src/SprykerFeature/Shared/Library/Autoloader.php');
} elseif (is_dir($vendor = __DIR__ . '/../../../../vendor')) {
    require($vendor . '/spryker/zed-package/src/SprykerFeature/Shared/Library/Autoloader.php');
} else {
    echo(
        'You must set up the project dependencies, run the following commands:' . PHP_EOL.
        'curl -s http://getcomposer.org/installer | php' . PHP_EOL .
        'php composer.phar install' . PHP_EOL
    );
    exit(1);
}

Autoloader::register(realpath($vendor . '/..'), $vendor);

$bootstrap = new SprykerFeature\Zed\Console\Communication\ConsoleBootstrap();
$bootstrap->run();
