machine:
  timezone:
    Europe/Berlin
  php:
    version: 5.6.5
  environment:
    APPLICATION_ENV: development
    APPLICATION_STORE: DE
  services:
    - elasticsearch
test:
  override:
    - bash circle_build.sh ${CIRCLE_BRANCH}
