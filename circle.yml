machine:
  php:
    version: 5.6.5
  environment:
    APPLICATION_ENV: development
    APPLICATION_STORE: DE

dependencies:
  override:
    - exit 0

test:
  override:
    - bash php .circleBuild ${CIRCLE_BRANCH} ${CIRCLE_USERNAME} ${API_TOKEN}
