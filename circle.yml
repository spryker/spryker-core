machine:
  php:
    version: 5.6.5
  environment:
    APPLICATION_ENV: development
    APPLICATION_STORE: DE
  services:
    - elasticsearch
test:
  override:
    - bash php .circleBuild ${CIRCLE_BRANCH} ${API_TOKEN}
